export interface Hero{
    text: string;
    title: string;
    subtitle: string;
    img: string;
    name: string;
}

export interface Skill{
    title: string;
    text: string;
    img: string;
}

export interface Project{
    title: string;
    text: string;
    img: string;
    id: number;
}
export interface Value{
    title: string;
    text: string;
    img: string;
}